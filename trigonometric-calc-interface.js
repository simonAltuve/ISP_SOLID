const sine = {
    CalculateSine(degrees) {
        return 'this is the seno';
    }
}

const cosine = {
    CalculateCosine (degrees) {
        return 'this is the cosine';
    }
}

module.exports = { sine, cosine }